query common_list(
  $path: String!
  $refreshToken: String
  $loadMoreToken: String
  $params: String
) {
  commonList(
    path: $path
    refreshToken: $refreshToken
    loadMoreToken: $loadMoreToken
    params: $params
  ) {
    ...ListFragment
  }
}
